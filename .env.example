# Safe local defaults only. Put credentials in ignored .env.local files or a secret store.
POSTGRES_DB=thinkso
POSTGRES_USER=thinkso
POSTGRES_PASSWORD=local-development-only
DATABASE_URL=postgresql+psycopg://thinkso:local-development-only@localhost:5432/thinkso
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
